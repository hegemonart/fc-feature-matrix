---
phase: 02-hospitality-pilot
plan: 13
type: execute
wave: 10
depends_on: [02-12]
files_modified:
  - app/MatrixIsland.tsx
  - app/hospitality/page.tsx
  - app/hospitality/HospitalityIsland.tsx
  - analysis/hospitality/features.ts
  - analysis/hospitality/results/.gitkeep
  - lib/data.ts
  - tests/visual/hospitality-tab.spec.ts
  - tests/visual/hospitality-tab.spec.ts-snapshots/
  - tests/components/HospitalityIsland.test.tsx
autonomous: true
requirements: [HOSP-03]
must_haves:
  truths:
    - "Visiting `/hospitality` in the running Next.js app shows a 5-club × 55-feature matrix with the label 'Pilot: 5 clubs' visible above the matrix"
    - "The Hospitality Packages entry in TopNav (LOCKED_TABS) routes to `/hospitality` for ANY authenticated user (signed-in OR premium OR admin) — clicking does NOT show the 'coming soon' / 'locked' modal anymore"
    - "Unauthenticated users still see the locked-modal (preserves existing matrix preview/auth behavior)"
    - "analysis/hospitality/features.ts buildPresence() reads from analysis/hospitality/results/{club}.json (5 imports) instead of the placeholder always-absent stub"
    - "Single-orange-CTA invariant preserved: the new /hospitality page contains AT MOST one element styled with `background: var(--accent)` (#FF490C). Verified in HospitalityIsland.test.tsx."
    - "Score-data invariant: `git diff --quiet analysis/homepage/` exits 0; `lib/scoring.ts` unchanged"
    - "`tests/visual/hospitality-tab.spec.ts` baseline established via `npx playwright test --update-snapshots` post-manual-diff-review"
  artifacts:
    - path: app/hospitality/page.tsx
      provides: "Server Component shell that loads pilot hospitality data + passes to HospitalityIsland"
      min_lines: 30
    - path: app/hospitality/HospitalityIsland.tsx
      provides: "Client Component rendering the 5-club × 55-feature matrix with 'Pilot: 5 clubs' label"
      min_lines: 80
    - path: analysis/hospitality/features.ts
      provides: "buildPresence() now imports from analysis/hospitality/results/*.json (5 clubs); removes placeholder stub"
      contains: "import mancity from './results/mancity.json'"
    - path: tests/visual/hospitality-tab.spec.ts
      provides: "Visual regression baseline + a11y check for /hospitality"
      min_lines: 30
    - path: tests/components/HospitalityIsland.test.tsx
      provides: "Single-orange-CTA invariant test + presence-mapping unit test"
      min_lines: 40
  key_links:
    - from: analysis/hospitality/results/{club}.json (Plan 02-12)
      to: analysis/hospitality/features.ts buildPresence()
      via: "Direct JSON import per homepage features.ts pattern"
      pattern: "import.*from './results/"
    - from: analysis/hospitality/features.ts FEATURES export
      to: lib/data.ts
      via: "lib/data.ts exports HOSPITALITY_FEATURES alongside FEATURES (homepage)"
      pattern: "HOSPITALITY_FEATURES"
    - from: app/MatrixIsland.tsx LOCKED_TABS handleTabClick
      to: app/hospitality/page.tsx
      via: "When user is authed AND tabId === 'hospitality', router.push('/hospitality') instead of showing locked modal"
      pattern: "/hospitality"
---

<objective>
Unlock the Hospitality Packages tab for the back-half pilot. The current behavior in `app/MatrixIsland.tsx` is: clicking any LOCKED_TABS entry opens a "locked" / "coming soon" modal regardless of auth state. This plan unlocks the `hospitality` tab entry so authenticated users navigate to a real `/hospitality` route showing the 5-club pilot matrix with explicit "Pilot: 5 clubs" labeling.

**Scope:**
1. Wire `analysis/hospitality/features.ts buildPresence()` to read from the 5 `analysis/hospitality/results/*.json` files (Plan 02-12 outputs) — currently it returns 'absent' for every product.
2. Add `HOSPITALITY_FEATURES` export to `lib/data.ts` (mirrors homepage `FEATURES` export).
3. Create `app/hospitality/page.tsx` (Server Component) + `app/hospitality/HospitalityIsland.tsx` (Client Component) following the homepage RSC→Client island split per `infra-redesign-v2`.
4. Add `Pilot: 5 clubs` label as a `mono-caption` chip directly above the matrix.
5. Modify `app/MatrixIsland.tsx::handleTabClick`: when `tabId === 'hospitality'` AND user is authed (any tier — signed-in, premium, admin), `router.push('/hospitality')` instead of showing the locked/coming-soon modal. Unauthenticated users still see the locked modal (preserves existing preview-mode UX).
6. Add a small `<TopNav back to home>` affordance on `/hospitality` so the user can return to the homepage matrix.

**Design system invariants (from CLAUDE.md):**
- **Single orange CTA per surface:** the `/hospitality` page contains AT MOST one element styled with `background: var(--accent)`. The current matrix has none in the table chrome (orange is reserved for primary CTAs). Hospitality matrix follows the same rule. The "Back to home" affordance MUST be text-only or white-outlined, NOT orange.
- **Design tokens only:** all colors via `var(--bg-page)`, `var(--bg-cell)`, `var(--text)`, `var(--accent)`. No hex literals in chrome.
- **Type stack:** `var(--font-body)` and `var(--font-mono)` (no literal `font-family` declarations).
- **Score-data invariant:** NEVER touch `analysis/homepage/results/*.json`, `lib/scoring.ts`, or `analysis/homepage/features.ts`. Verification gate: `git diff --quiet analysis/homepage/`.

Out of scope: any homepage matrix changes; any change to LOCKED_TABS array structure (only the `hospitality` entry's click handler is special-cased — the array stays).

Source-decision traceability:
- D-19: Hospitality tab unlocked in app, visibly marked "Pilot: 5 clubs" until Phase 2.5.
- HOSP-03: results JSON per pilot club + Hospitality Packages tab unlocked.
- CLAUDE.md design-system rules: single-orange-CTA, design tokens, type stack, visual regression baseline update only when intentional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-hospitality-pilot/02-CONTEXT.md
@.planning/phases/02-hospitality-pilot/02-12-PLAN.md
@CLAUDE.md
@app/MatrixIsland.tsx
@app/page.tsx
@app/layout.tsx
@app/globals.css
@app/components
@analysis/hospitality/features.ts
@analysis/homepage/features.ts
@lib/data.ts
@types.ts
@products.ts
@tests/visual/homepage.spec.ts
@tests/components/Modal.test.tsx

<interfaces>
<!-- Existing homepage features.ts buildPresence pattern (verified): -->

```typescript
// analysis/homepage/features.ts (lines ~50-60 — used as the pattern):
import man_city from './results/man_city.json';
import tottenham from './results/tottenham.json';
// ... etc

const RESULTS: Record<string, Record<string, boolean>> = {
  man_city: man_city.features,
  tottenham: tottenham.features,
  // ... etc
};

function buildPresence(featureKey: string): Record<string, PresenceStatus> {
  const m: Record<string, PresenceStatus> = {};
  ALL_IDS.forEach(id => {
    m[id] = RESULTS[id]?.[featureKey] === true ? 'present' : 'absent';
  });
  return m;
}
```

<!-- Plan 02-13 mirrors this in analysis/hospitality/features.ts: -->

```typescript
// analysis/hospitality/features.ts — Plan 02-13 replaces the placeholder
import mancity from './results/mancity.json';
import tottenham from './results/tottenham.json';
import realmadrid from './results/realmadrid.json';
import psg from './results/psg.json';
import chelsea from './results/chelsea.json';

const PILOT_RESULTS: Record<string, Record<string, boolean>> = {
  mancity: mancity.features,
  tottenham: tottenham.features,
  real_madrid: realmadrid.features,    // map to product_id (matches products.ts)
  psg: psg.features,
  chelsea: chelsea.features,
};

function buildPresence(featureKey: string): Record<string, PresenceStatus> {
  const m: Record<string, PresenceStatus> = {};
  ALL_IDS.forEach(id => {
    // Pilot covers 5 clubs only; non-pilot product IDs stay 'absent'
    m[id] = PILOT_RESULTS[id]?.[featureKey] === true ? 'present' : 'absent';
  });
  return m;
}
```

<!-- LOCKED_TABS handler change (app/MatrixIsland.tsx:406-430): -->

```typescript
// CURRENT (preserve UNCHANGED for non-hospitality LOCKED_TABS entries):
const handleTabClick = useCallback((tabId: string) => {
  if (tabId === 'home') return;
  // Plan 02-13 SPECIAL-CASE: hospitality is unlocked for any authed user
  if (tabId === 'hospitality' && (authed || isPremium || isAdmin)) {
    trackEvent('tab_click', { tab: 'Hospitality Packages', outcome: 'navigate' });
    router.push('/hospitality');
    return;
  }
  // Existing logic for all other tabs UNCHANGED:
  const tabName = tabId === 'unlock' ? 'Premium' : LOCKED_TABS.find(t => t.id === tabId)?.name ?? tabId;
  if (isAdmin || isPremium) {
    // ... coming soon modal (existing path) ...
  } else if (authed) {
    // ... locked modal (existing path) ...
  } else {
    // ... locked modal (existing path) ...
  }
}, [authed, isAdmin, isPremium, router]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire buildPresence() to results JSONs + add HOSPITALITY_FEATURES export to lib/data.ts</name>
  <files>
    analysis/hospitality/features.ts,
    lib/data.ts,
    tests/components/HospitalityIsland.test.tsx
  </files>
  <behavior>
    - Test 1: After Plan 02-12 wrote `analysis/hospitality/results/*.json`, importing `analysis/hospitality/features.ts` does not throw on TypeScript compile.
    - Test 2: `FEATURES[0].presence['man_city']` (or 'mancity' — match products.ts ID for Manchester City) returns `'present'` or `'absent'` based on the actual results JSON value (NOT always 'absent').
    - Test 3: For products NOT in the pilot 5 (e.g. `arsenal`, `liverpool`), every feature's presence is `'absent'` (pilot coverage only).
    - Test 4: `lib/data.ts` exports `HOSPITALITY_FEATURES` of type `Feature[]` with length 55.
    - Test 5: `npx tsc --noEmit` exits 0.
  </behavior>
  <action>
    1. **Replace `buildPresence` in `analysis/hospitality/features.ts`**:
       - Add 5 JSON imports at the top (one per pilot club).
       - Build `PILOT_RESULTS` keyed by product_id (look up the actual product_id slugs in `products.ts` — recommend `mancity`, `tottenham`, `real_madrid`, `psg`, `chelsea` matching the homepage convention; verify at execute time).
       - The placeholder `buildPresence(_featureKey)` becomes the real one (signature unchanged for callers).
       - Preserve all 55 `feat(...)` calls VERBATIM. The HP01..HP55 IDs and tier weights stay.
       - Verify the products.ts product_id mapping matches what Plan 02-12's derive_results_json wrote in the result JSON's `club_id` field. If mismatch (e.g. derive used `mancity` but products.ts uses `man_city`), modify the PILOT_RESULTS map keys to match products.ts (canonical source).
    2. **Extend `lib/data.ts`**:
       - Add `import { FEATURES as HOSPITALITY_FEATURES_SRC } from '@/analysis/hospitality/features';`
       - Export `HOSPITALITY_FEATURES = HOSPITALITY_FEATURES_SRC` for the new `/hospitality` route to consume.
       - DO NOT touch the existing `FEATURES` export (homepage source unchanged — D-20).
    3. **Add `.gitkeep`** to `analysis/hospitality/results/` if the directory is empty pre-Plan-02-12 (it shouldn't be by execute time — Plan 02-12 already populated it). Skip if not needed.
    4. **Tests** (`tests/components/HospitalityIsland.test.tsx` — set up the file even though Task 2 fills the rest):
       - Tests 1–4 from <behavior>. Use vitest + RTL where applicable.
       - For Test 2: need to know the actual presence values. Approach: import the module at test time, snapshot a known feature key (e.g. `package_tier_list` from HP01) for `mancity` and assert it's a known value (whatever Plan 02-12 derived). Brittle but acceptable for back-half — the test will need updating if the underlying results change.
    5. Verify D-20: `git diff --quiet analysis/homepage/` exits 0 after the change.
    6. Commits:
       - `feat(02-13): analysis/hospitality/features.ts buildPresence() reads pilot result JSONs`
       - `feat(02-13): lib/data.ts exports HOSPITALITY_FEATURES`
       - `test(02-13): hospitality features wiring + lib/data export tests`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10 && npx vitest run tests/components/HospitalityIsland.test.tsx 2>&1 | tail -15 && git diff --quiet analysis/homepage/ && echo "D-20 OK"</automated>
  </verify>
  <done>
    - `analysis/hospitality/features.ts` reads from results JSONs.
    - `lib/data.ts` exports `HOSPITALITY_FEATURES`.
    - `npx tsc --noEmit` passes.
    - 5 tests pass.
    - D-20 invariant intact.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /hospitality route — Server Component + Client island + matrix render with "Pilot: 5 clubs" label</name>
  <files>
    app/hospitality/page.tsx,
    app/hospitality/HospitalityIsland.tsx,
    app/MatrixIsland.tsx,
    tests/components/HospitalityIsland.test.tsx,
    tests/visual/hospitality-tab.spec.ts,
    tests/visual/hospitality-tab.spec.ts-snapshots/
  </files>
  <behavior>
    - Test 1 (component): `<HospitalityIsland>` renders 5 product columns + 55 feature rows. The label "Pilot: 5 clubs" appears as a `mono-caption` chip above the matrix.
    - Test 2 (single-orange-CTA): `tests/components/HospitalityIsland.test.tsx` queries `document.querySelectorAll('[style*="--accent"], .orange-cta')` (or whatever existing test pattern is — borrow from `tests/components/Modal.test.tsx`) and asserts ≤ 1 orange-styled element on the page. (The "Back to home" link MUST NOT be orange.)
    - Test 3 (route): visiting `/hospitality` server-side returns 200 (build-time check via `npx next build`).
    - Test 4 (visual baseline): `tests/visual/hospitality-tab.spec.ts` runs Playwright at viewport 1440×900, captures the page, compares against the committed PNG snapshot with `maxDiffPixelRatio: 0.02`. Initial run uses `--update-snapshots` to BUILD the baseline; once committed, subsequent runs verify.
    - Test 5 (a11y): visual spec also runs an axe-core check (mirrors `homepage.spec.ts` Lighthouse a11y posture). Aim for parity.
    - Test 6 (tab routing): clicking the `Hospitality Packages` tab in TopNav as an AUTHED user navigates to `/hospitality` (no locked modal). As an UNAUTHED user, the locked modal still appears.
  </behavior>
  <action>
    1. **Create `app/hospitality/page.tsx`** (~40 lines):
       - Server Component (mirrors `app/page.tsx` pattern).
       - Imports `HOSPITALITY_FEATURES` from `lib/data.ts` and `PRODUCTS` (or `PILOT_PRODUCTS = PRODUCTS.filter(p => ['mancity','tottenham','real_madrid','psg','chelsea'].includes(p.id))`).
       - Computes scores via `getProductScores(p.id, HOSPITALITY_FEATURES)` if `lib/scoring.ts` accepts a feature array; OR computes inline by summing weight×presence per feature for the pilot products.
       - **DO NOT modify `lib/scoring.ts`** (D-20 / score-data invariant). If `getProductScores` is hardcoded to homepage `FEATURES`, write a small inline tally inside `page.tsx` rather than refactoring the scoring lib.
       - Passes `pilotProducts`, `hospitalityFeatures`, `scores` to `<HospitalityIsland>`.
       - Resolves `BUILD_DATE` same way as `app/page.tsx`.
    2. **Create `app/hospitality/HospitalityIsland.tsx`** (~120 lines):
       - Client Component. Renders the matrix in the same layout as the homepage (`<TopNav>` + `<Sidebar>` + `<table>` matrix), but only with the 5 pilot clubs and 55 hospitality features.
       - Above the matrix, render a label:
         ```tsx
         <div className="mono-caption pilot-label">Pilot: 5 clubs</div>
         ```
         The label uses the existing `mono-caption` style (10/13/-1px Roboto Mono per `globals.css`). Add `.pilot-label` class with NO orange-accent styling.
       - Reuse existing matrix components: `<DataCell>`, `<SortHeader>`, `<MeterRow>`, `<HoverTooltipCard>`, `<TableRows>` — same as homepage.
       - **No "unlock everything" CTA on the hospitality page** (that's homepage-specific). The single-orange-CTA budget on `/hospitality` is reserved for a future primary action; for now zero orange CTAs is acceptable.
       - Add a "Back to home" affordance — a plain `<Link>` to `/` styled text-only or with `var(--border)` outline. NEVER orange.
       - On mount: `trackEvent('page_view', { path: '/hospitality' })` (matches homepage pattern).
    3. **Modify `app/MatrixIsland.tsx`**:
       - In `handleTabClick`, special-case `hospitality` BEFORE the existing `if (isAdmin || isPremium)` branch: when `tabId === 'hospitality'` AND `(authed || isPremium || isAdmin)`, `router.push('/hospitality')` (use Next's `useRouter`). Add `import { useRouter } from 'next/navigation';` if not already present.
       - For unauthed users, FALL THROUGH to the existing `else` branch (locked modal). Behavior preserved.
       - DO NOT remove `hospitality` from `LOCKED_TABS` array — the array represents tab definitions, not click handlers. Removing would break the rendering.
    4. **Visual regression baseline** (`tests/visual/hospitality-tab.spec.ts`):
       - Mirror `tests/visual/homepage.spec.ts` shape (read it first to match the test posture, snapshot strategy, and `maxDiffPixelRatio: 0.02` config).
       - Test 1: navigate to `/hospitality`, full-page screenshot, compare to baseline PNG.
       - Test 2 (a11y): basic axe-core scan post-load — fail on any violations of impact ≥ moderate (matches homepage spec).
       - Run with `npx playwright test tests/visual/hospitality-tab.spec.ts --update-snapshots` after manual visual diff review (per CLAUDE.md design-system rule). The intentional-change comment goes in the commit message.
    5. **Component test** (`tests/components/HospitalityIsland.test.tsx`):
       - Tests 1, 2, 6 from <behavior>.
       - Use vitest + @testing-library/react.
       - For Test 2 (single-orange-CTA): borrow the assertion pattern from `tests/components/Modal.test.tsx` — recommend reading it FIRST so the test posture matches.
       - Test 6 needs the auth context mocked (mirror the existing matrix test patterns; `tests/components/HeaderBar.test.tsx` likely has the auth-mock pattern).
    6. **Build verification:**
       - `npx next build` must succeed with `/hospitality` as a new route.
       - `npx tsc --noEmit` must pass.
       - `npx vitest run` must pass (existing + new tests).
       - `npx playwright test tests/visual/hospitality-tab.spec.ts` (post-baseline) must pass.
    7. **D-20 + design-invariant verification:**
       - `git diff --quiet analysis/homepage/` exits 0.
       - `git diff lib/scoring.ts` empty (NOT modified).
       - Manual review: `app/hospitality/HospitalityIsland.tsx` contains AT MOST one element with `var(--accent)` background — the test enforces but eyeball-verify on first build.
    8. Commits:
       - `feat(02-13): app/hospitality route — Server + Client island with Pilot: 5 clubs label`
       - `feat(02-13): MatrixIsland routes hospitality tab to /hospitality for authed users`
       - `test(02-13): HospitalityIsland component tests + visual baseline + a11y check`
       - `chore(02-13): commit Playwright visual baseline for /hospitality (intentional design-change snapshot update)`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10 && npx next build 2>&1 | tail -20 && npx vitest run tests/components/HospitalityIsland.test.tsx 2>&1 | tail -15 && npx playwright test tests/visual/hospitality-tab.spec.ts 2>&1 | tail -15 && git diff --quiet analysis/homepage/ && git diff --quiet lib/scoring.ts && echo "D-20 + score-invariant OK"</automated>
  </verify>
  <done>
    - `/hospitality` route exists and renders the 5-club × 55-feature matrix with "Pilot: 5 clubs" label.
    - `app/MatrixIsland.tsx` routes authed users to `/hospitality` when clicking the Hospitality tab; unauthed users still see the locked modal.
    - Single-orange-CTA invariant preserved (test passes).
    - `npx next build` succeeds.
    - `npx tsc --noEmit` passes.
    - Visual baseline committed (PNG under `tests/visual/hospitality-tab.spec.ts-snapshots/`).
    - 6 component / visual tests pass.
    - D-20 + score-data invariant intact.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analysis/hospitality/results/*.json → buildPresence | Trusted (Plan 02-12 output, committed). |
| /hospitality route → unauthenticated users | Existing matrix preview-mode policy preserved (locked modal); no policy bypass. |
| Visual baseline PNG → CI | Committed snapshot; PR review required for changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | /hospitality unauth fall-through | mitigate | The route is public-readable (just like /club/[id]); the existing matrix preview-blur covers locked-tier data. Pilot data is benchmark-public (no PII). |
| T-13-02 | Tampering | analysis/hospitality/features.ts buildPresence | accept | TypeScript-compile-checked at build time. Result JSONs are committed and reviewed in PR. |
| T-13-03 | Spoofing | LOCKED_TABS click handler bypass | accept | The hospitality special-case is gated on `authed || isPremium || isAdmin` — same access surface as the matrix preview. Adding hospitality to a non-LOCKED list would expose it to unauthed users; we intentionally keep it in LOCKED_TABS so the unauthed flow stays unchanged. |
| T-13-04 | Denial of Service | Visual snapshot drift | accept | `maxDiffPixelRatio: 0.02` allows small renderer drift. Intentional design changes require explicit `--update-snapshots` commit. |
| T-13-05 | Repudiation | trackEvent('page_view', '/hospitality') | accept | Mirrors homepage analytics; user awareness of tracking is governed by existing privacy notice. |

</threat_model>

<verification>
- [ ] `app/hospitality/page.tsx` + `app/hospitality/HospitalityIsland.tsx` exist.
- [ ] `analysis/hospitality/features.ts buildPresence` reads pilot result JSONs.
- [ ] `lib/data.ts` exports `HOSPITALITY_FEATURES` of length 55.
- [ ] `app/MatrixIsland.tsx::handleTabClick` routes authed hospitality clicks to `/hospitality`.
- [ ] Single-orange-CTA invariant test passes.
- [ ] Visual baseline PNG committed under `tests/visual/hospitality-tab.spec.ts-snapshots/`.
- [ ] a11y check passes (no impact ≥ moderate violations).
- [ ] `npx next build` exits 0.
- [ ] `npx tsc --noEmit` exits 0.
- [ ] `npx vitest run` all green.
- [ ] D-20: `git diff --quiet analysis/homepage/` exits 0.
- [ ] Score-data invariant: `git diff --quiet lib/scoring.ts` exits 0.
</verification>

<success_criteria>
- Hospitality Packages tab unlocked end-to-end for authed users.
- "Pilot: 5 clubs" label visible above matrix.
- 5 pilot clubs × 55 features rendered correctly with present/absent state from results JSONs.
- Design-system invariants intact (single-orange-CTA, design tokens, type stack, score-data invariance).
- Visual regression baseline committed.
- a11y baseline maintained.
- Subscription cost: $0.
</success_criteria>

<output>
After completion, create `.planning/phases/02-hospitality-pilot/02-13-SUMMARY.md` with:
- Route URL + screenshot.
- Pilot products + feature counts.
- Visual diff summary (snapshot baseline establishment commit hash).
- Lighthouse / axe-core a11y score.
- Single-orange-CTA invariant verification statement.
- D-20 + score-data invariant gate exit codes.
- Commits list.
</output>
