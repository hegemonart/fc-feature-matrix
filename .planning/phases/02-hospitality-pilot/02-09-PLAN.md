---
phase: 02-hospitality-pilot
plan: 09
type: execute
wave: 6
depends_on: [02-08]
files_modified:
  - scanner/flow-maps/hospitality/mancity.json
  - scanner/flow-maps/hospitality/tottenham.json
  - scanner/flow-maps/hospitality/realmadrid.json
  - scanner/flow-maps/hospitality/psg.json
  - scanner/flow-maps/hospitality/chelsea.json
  - scanner/flow/schema.py
  - scanner/tests/test_flow_schema_overrides.py
autonomous: true
requirements: [HOSP-01]
must_haves:
  truths:
    - "All 5 pilot flow-maps now describe the FULL purchase flow (landing → category → package-detail → match-selector → enquiry-form-prefill, pre-submit) per D-08"
    - "Each flow-map step that requires a logged-in session is marked `requires_credentials: true` (additive FlowStep field) so the capture orchestrator authenticates BEFORE that step"
    - "Steps the crawler couldn't reach due to bot-challenges (MCFC + PSG-billetterie + RMA) are marked `manual_chrome_mcp: true` so the capture orchestrator (Plan 02-10) prompts the user to drive Chrome MCP for that step instead of Playwright"
    - "Chelsea hospitality.chelseafc.com paid-customer-gated steps are marked `skipped: requires-paid-account` (Option B partial decision, 2026-04-27)"
    - "All 5 flow-maps still validate against FlowMap schema (`scanner flow validate <each>` exits 0)"
    - "All 5 flow-maps satisfy D-08 click-through (landing → category → package → match → enquiry-form, pre-submit, NO submit per D-16)"
  artifacts:
    - path: scanner/flow-maps/hospitality/mancity.json
      provides: "Manchester City extended flow-map: landing (Chrome MCP for Cloudflare bypass) → 4 tier landings → match-selector → enquiry form prefill"
      min_steps: 8
    - path: scanner/flow-maps/hospitality/tottenham.json
      provides: "Tottenham extended flow-map: landing → matchday-options → premium-seats detail → match-selector → enquiry form prefill"
      min_steps: 10
    - path: scanner/flow-maps/hospitality/realmadrid.json
      provides: "Real Madrid extended flow-map: VIP-area landing → matchday-hospitality (Chrome MCP for CAPTCHA) → palcos-vip → match-selector"
      min_steps: 8
    - path: scanner/flow-maps/hospitality/psg.json
      provides: "PSG extended flow-map: www.psg.fr landing → /en/hospitality category → All Executive Club detail → match-selector → enquiry-form (billetterie steps Chrome-MCP-marked)"
      min_steps: 10
    - path: scanner/flow-maps/hospitality/chelsea.json
      provides: "Chelsea extended flow-map: hospitality.chelseafc.com landing → 5 package landings (Centenary/Platinum/Home Dugout/Museum/Tambling) → checkout-step (skipped:requires-paid-account)"
      min_steps: 8
    - path: scanner/flow/schema.py
      provides: "Additive FlowStep fields: requires_credentials, manual_chrome_mcp, skipped (each Optional/default-False/None)"
      contains: "requires_credentials"
  key_links:
    - from: scanner/flow-maps/hospitality/*.json
      to: scanner/flow/schema.py
      via: "Pydantic model_validate() at scanner.flow.validate.validate_flow_map call site"
      pattern: "validate_flow_map"
    - from: scanner/flow-maps/hospitality/*.json
      to: scanner/capture/capture.py (Plan 02-10)
      via: "FlowStep.requires_credentials triggers credentials.get_credential() lookup before that step"
      pattern: "requires_credentials"
---

<objective>
Author per-club flow-map override drafts that EXTEND the front-half crawler-discovered flow-maps (which stopped at depth 3 / 12 steps before reaching purchase flow CTAs) into the FULL D-08 purchase path:

`landing → experience-category → package-detail → match-selector → enquiry-form (pre-submit, never submitted per D-16)`

This is largely manual JSON editing work — about 45 min per club × 5 clubs ≈ 3.75 hrs. Crawler-discovered steps remain as the seed; this plan appends explicit click-paths.

Per-club specifics:
- **Manchester City:** Cloudflare Turnstile blocks Playwright entry → mark landing step `manual_chrome_mcp: true`. Then 4 tier landings (Tunnel Club Premier, Tunnel Club Ground, Backstage, Your Matchday Experience), match-selector with first-fixture rule (D-09), enquiry form prefill (D-10).
- **Tottenham:** cleanest crawl path, no CAPTCHAs. Extends to 10–12 steps covering Premium Seats / Broadcast Booth / Stratus tiers + matchday-options sub-landing.
- **Real Madrid:** VIP-area landing reachable, but CAPTCHA halts descent. Mark CAPTCHA-protected steps `manual_chrome_mcp: true`. Cover Matchday Hospitality + Palcos VIP branches.
- **PSG:** www.psg.fr/en/hospitality landing reachable. Mark deeper billetterie.psg.fr steps `manual_chrome_mcp: true` (Cloudflare on subdomain). Login-gated steps `requires_credentials: true`.
- **Chelsea:** hospitality.chelseafc.com landing reachable. Per Option B partial decision (2026-04-27): mark deep steps requiring paid-customer status as `skipped: requires-paid-account`. Capture what we can — main domain pages + public package landing pages on subdomain.

Source-decision traceability:
- D-08 click-through pattern.
- D-09 match-selector picks first available fixture.
- D-10 enquiry forms filled with `Test Test` / `test@example.com` / `+44 0000 000000` — never submitted.
- D-15 login beyond reasonable dummy account → flag and skip.
- D-16 schema bans `submit` action (closed Literal).
- 02-BACK-HALF-HANDOFF.md "Chelsea Option B partial decision (2026-04-27)" — paid-customer pages marked skipped.
- 02-RESEARCH.md §5 (per-club friction notes).
- 02-06-CRAWL-LOG.md (registration matrix per club).

Subscription cost: $0 (manual JSON authoring + zero new vision/SDK calls).

Output: 5 production-ready flow-maps that the Plan 02-10 capture orchestrator can consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-hospitality-pilot/02-CONTEXT.md
@.planning/phases/02-hospitality-pilot/02-RESEARCH.md
@.planning/phases/02-hospitality-pilot/02-FRONT-HALF-SUMMARY.md
@.planning/phases/02-hospitality-pilot/02-BACK-HALF-HANDOFF.md
@.planning/phases/02-hospitality-pilot/02-06-CRAWL-LOG.md
@.planning/phases/02-hospitality-pilot/02-08-PLAN.md
@scanner/flow/schema.py
@scanner/flow-maps/hospitality/mancity.json
@scanner/flow-maps/hospitality/tottenham.json
@scanner/flow-maps/hospitality/realmadrid.json
@scanner/flow-maps/hospitality/psg.json
@scanner/flow-maps/hospitality/chelsea.json

<interfaces>
<!-- Schema additions this plan introduces (additive default-factory pattern) -->

```python
# scanner/flow/schema.py — Plan 02-09 additive
class FlowStep(BaseModel):
    step_name: str
    url: str | None = None
    action: Literal["navigate", "click", "fill", "wait", "screenshot"]
    selector: str | None = None
    form_fields: list[FormField] | None = None
    hide_selectors: list[str] = Field(default_factory=list)
    wait_for: str | None = None
    # Plan 02-09 ADDS:
    requires_credentials: bool = False     # capture orchestrator authenticates before this step
    manual_chrome_mcp: bool = False        # capture orchestrator pauses; user drives Chrome MCP
    skipped: str | None = None             # e.g. "requires-paid-account" — capture skips this step entirely
```

D-16 invariant unchanged: action Literal still bans "submit". The new fields are additive metadata that the capture orchestrator reads.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema additions for FlowStep override fields + per-club flow-map drafts (mancity + tottenham)</name>
  <files>
    scanner/flow/schema.py,
    scanner/flow-maps/hospitality/mancity.json,
    scanner/flow-maps/hospitality/tottenham.json,
    scanner/tests/test_flow_schema_overrides.py
  </files>
  <behavior>
    - Test 1: A FlowStep WITHOUT the three new fields validates (default values: requires_credentials=False, manual_chrome_mcp=False, skipped=None). Pre-existing flow-maps still load.
    - Test 2: A FlowStep WITH `requires_credentials: true` validates and round-trips through model_dump() preserving the field.
    - Test 3: A FlowStep WITH `skipped: "requires-paid-account"` validates; the value is preserved as a string.
    - Test 4: D-16 invariant preserved: a FlowStep with `action: "submit"` STILL fails validation (closed Literal unchanged).
    - Test 5: `scanner flow validate scanner/flow-maps/hospitality/mancity.json` exits 0; `len(steps) >= 8`.
    - Test 6: `scanner flow validate scanner/flow-maps/hospitality/tottenham.json` exits 0; `len(steps) >= 10`.
    - Test 7: Tottenham flow-map's enquiry step has `form_fields` populated with D-10 dummy values (`"Test Test"` etc.), `action: "fill"`, NOT `"submit"`.
  </behavior>
  <action>
    1. **Schema** (`scanner/flow/schema.py`): extend FlowStep with three additive fields (default values shown in interfaces block above). Pre-existing flow-maps must validate unchanged. Run all 5 front-half flow-maps through `validate_flow_map` to confirm.

    2. **Manchester City flow-map** (`scanner/flow-maps/hospitality/mancity.json`):
       Replace the current 3-step entry-only stub with an 8–12-step extended path. Read the existing file first to preserve metadata (broker_vendor, dead_ends from Cloudflare 403, etc.) and ADD steps. Schema cap = 15. Suggested step list:
       - Step 1 `landing` — `navigate` to `https://www.mancity.com/hospitality`, `manual_chrome_mcp: true`, hide_selectors include common chat/cookie residue. Reason: Cloudflare Turnstile blocks Playwright (02-06-CRAWL-LOG.md §Manchester City).
       - Step 2 `cookie_dismiss` — `wait`, wait_for=`networkidle`. (Chrome MCP handles cookies.)
       - Step 3 `screenshot_landing` — `screenshot`.
       - Steps 4–7 — `navigate` to each tier: `/the-tunnel-club-premier`, `/the-tunnel-club`, `/backstage`, `/your-matchday-experience`. Each followed by a `screenshot`. Mark each `manual_chrome_mcp: true` since the bot-challenge persists.
       - Step 8 `match-selector` — `click` on first fixture card; `manual_chrome_mcp: true`.
       - Step 9 `enquiry-form-prefill` — `fill` step, `form_fields: [{selector: "input[name='name']", value: "Test Test"}, {selector: "input[name='email']", value: "test@example.com"}, {selector: "input[name='phone']", value: "+44 0000 000000"}]`. NO submit (D-16). `manual_chrome_mcp: true` (form is behind the challenge).
       - Step 10 `screenshot_form_prefilled` — `screenshot`.
       - Update `metadata.fixture_id` to a placeholder `"FIRST_AVAILABLE"` (D-09 rule — capture orchestrator records the actual ID).
       - PRESERVE existing metadata.dead_ends, broker_vendor, captcha_encountered values.

    3. **Tottenham flow-map** (`scanner/flow-maps/hospitality/tottenham.json`):
       Tottenham was the cleanest crawl (12 steps, no CAPTCHA). Extend to 12–14 steps (cap 15). Build atop the front-half discovery:
       - Steps 1–3 — landing + cookie + screenshot (existing front-half).
       - Steps 4–6 — `/tickets/premium-experiences/matchday-options/` navigate → wait → screenshot.
       - Steps 7–9 — Premium Seats tier landing → wait → screenshot.
       - Steps 10–11 — match-selector click → screenshot. `requires_credentials: false` (research §5: TOT has transparent buy-direct paths; verify in 02-10).
       - Steps 12–14 — `enquiry-form-prefill` (D-10 dummy fields), `screenshot`. Watch the 15-step cap.
       - hide_selectors per step include OKX sponsor overlay (research §5) + livechat widgets (D-11).

    4. **Tests** (`scanner/tests/test_flow_schema_overrides.py`):
       - 4 schema tests (additive fields default + roundtrip + Submit-still-banned + 5-existing-maps regression).
       - 2 flow-map content tests (mancity step count + tottenham step count + form-prefill correctness).
       - One regression test re-validates the 5 front-half maps + the 2 newly extended ones via `validate_flow_map` programmatically.

    5. Commit per club, conventional pattern:
       - `feat(02-09): extend FlowStep schema with override fields (requires_credentials, manual_chrome_mcp, skipped)`
       - `feat(02-09): mancity flow-map extended to full purchase path (Chrome MCP marked)`
       - `feat(02-09): tottenham flow-map extended to enquiry form prefill`
       - `test(02-09): schema override tests + per-club flow-map validation`
  </action>
  <verify>
    <automated>cd scanner && uv run pytest tests/test_flow_schema_overrides.py -v 2>&1 | tail -20 && uv run python -m scanner flow validate flow-maps/hospitality/mancity.json && uv run python -m scanner flow validate flow-maps/hospitality/tottenham.json</automated>
  </verify>
  <done>
    - mancity.json has 8–15 steps, each Chrome-MCP-marked appropriately, `validate` exits 0.
    - tottenham.json has 10–15 steps, includes enquiry form prefill, `validate` exits 0.
    - Schema accepts the 3 new optional fields; D-16 closed Literal still bans `submit`.
    - All 7 flow-maps (5 front-half + 2 extended) validate.
    - Existing tests still pass; 7 new tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Real Madrid + PSG flow-map drafts</name>
  <files>
    scanner/flow-maps/hospitality/realmadrid.json,
    scanner/flow-maps/hospitality/psg.json,
    scanner/tests/test_flow_schema_overrides.py
  </files>
  <behavior>
    - Test 1: realmadrid.json validates, has ≥ 8 steps, includes both `vip-area/matchday-hospitality` AND `vip-area/seasonal-vip/palcos-vip` branches.
    - Test 2: realmadrid.json marks the post-CAPTCHA descent steps `manual_chrome_mcp: true`.
    - Test 3: psg.json validates, has ≥ 10 steps, includes www.psg.fr landing AND a billetterie.psg.fr step marked `manual_chrome_mcp: true`.
    - Test 4: psg.json's enquiry step (if reachable through www) has `requires_credentials: true` (per research §5: PSG hospitality enquiry typically requires login on www).
  </behavior>
  <action>
    1. **Real Madrid** (`scanner/flow-maps/hospitality/realmadrid.json`):
       Front-half hit a CAPTCHA after 3 steps. Extend with the EN-US path (D-21 says language baked into URL):
       - Steps 1–3 — landing `/en-US/vip-area/home` → cookie dismiss (RMA_STRATEGY) → screenshot.
       - Step 4 — `manual_chrome_mcp: true` `navigate` to `/en-US/vip-area/matchday-hospitality`. (CAPTCHA halts Playwright per 02-06-CRAWL-LOG.md.)
       - Steps 5–7 — wait + screenshot + click first-tier card. All `manual_chrome_mcp: true`.
       - Step 8 — `navigate` to `/en-US/vip-area/seasonal-vip/palcos-vip` (alternate branch). `manual_chrome_mcp: true`.
       - Steps 9–10 — wait + screenshot. Optional `match-selector` step if reachable.
       - Steps 11–12 — `enquiry-form-prefill` if surfaced; otherwise stop at screenshots. Mark `requires_credentials: true` (RMA likely gates seasonal-VIP enquiries).
       - Total step count: 8–12.
       - hide_selectors include `[id*="onetrust"]`, livechat (D-11).

    2. **PSG** (`scanner/flow-maps/hospitality/psg.json`):
       Front-half successfully crawled `www.psg.fr/en/hospitality` (12 steps, fallback path). Billetterie was Cloudflare-blocked. Extend with both paths:
       - Steps 1–4 — www.psg.fr/en/hospitality landing + cookie + screenshot + first-tier click. Standard Playwright (no Chrome MCP needed).
       - Steps 5–7 — `navigate` to a deeper www-path (e.g. `/en/hospitality/all-executive-club` if surfaced; otherwise the closest equivalent — confirm at execute time).
       - Step 8 — `navigate` to `https://billetterie.psg.fr/en/home-vip`. Mark `manual_chrome_mcp: true` (Cloudflare bot-challenge per 02-06-CRAWL-LOG.md).
       - Step 9 — `wait` + screenshot, `manual_chrome_mcp: true`.
       - Step 10 — login step on billetterie. `requires_credentials: true; manual_chrome_mcp: true`. The capture orchestrator (02-10) hands off to user with credentials surfaced.
       - Steps 11–13 — match-selector + enquiry-form-prefill. Both `manual_chrome_mcp: true` because they live on billetterie.
       - Total step count: 10–13.
       - hide_selectors include common French CMP overlays (Didomi/Axeptio per research §5).

    3. **Tests** (extend `scanner/tests/test_flow_schema_overrides.py`):
       - 1 test for realmadrid step count + manual_chrome_mcp markers on post-CAPTCHA steps.
       - 1 test for psg step count + dual-domain coverage (www + billetterie marked).
       - 1 test for psg login step has `requires_credentials: true`.
       - 1 test confirming D-09 fixture_id placeholder present in metadata for both.

    4. Commits:
       - `feat(02-09): realmadrid flow-map covers matchday + palcos-vip (CAPTCHA→Chrome MCP)`
       - `feat(02-09): psg flow-map covers www + billetterie (Cloudflare→Chrome MCP, requires_credentials)`
  </action>
  <verify>
    <automated>cd scanner && uv run python -m scanner flow validate flow-maps/hospitality/realmadrid.json && uv run python -m scanner flow validate flow-maps/hospitality/psg.json && uv run pytest tests/test_flow_schema_overrides.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>
    - realmadrid.json has 8–12 steps, validates, includes both VIP branches with manual_chrome_mcp markers.
    - psg.json has 10–13 steps, validates, includes www + billetterie split with manual_chrome_mcp + requires_credentials markers as appropriate.
    - All schema-override tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Chelsea flow-map (Option B partial — paid-customer steps marked skipped)</name>
  <files>
    scanner/flow-maps/hospitality/chelsea.json,
    scanner/tests/test_flow_schema_overrides.py
  </files>
  <behavior>
    - Test 1: chelsea.json validates, has ≥ 8 steps.
    - Test 2: At least one step has `skipped: "requires-paid-account"` (Option B partial decision per 02-BACK-HALF-HANDOFF.md 2026-04-27 update).
    - Test 3: chelsea.json's `metadata.dead_ends` does NOT contain duplicate URLs (per Plan 02-08 dedupe fix — verify by hand-inspection in test).
    - Test 4: chelsea.json includes the 5 main package landings (Centenary Club, Platinum, Home Dugout Club, Museum, Tambling) where reachable on hospitality.chelseafc.com without paid-customer status.
  </behavior>
  <action>
    1. **Chelsea** (`scanner/flow-maps/hospitality/chelsea.json`):
       Per Option B partial (2026-04-27): chelseafc.com main domain registration succeeded; hospitality.chelseafc.com is paid-customer-gated for full access. Capture WHAT WE CAN, mark the rest skipped.
       - Steps 1–3 — `https://hospitality.chelseafc.com/match-by-match-hospitality-packages` landing + cookie dismiss (CHE_STRATEGY) + screenshot. Standard Playwright.
       - Steps 4–8 — `navigate` to each of 5 package landings: Centenary, Platinum, Home Dugout Club, Museum suite, Tambling Suite (URL slugs verified at execute time via the front-half crawl results — extend if necessary).
       - Step 9 — `match-selector` step. Mark `skipped: "requires-paid-account"`. Reason: per 02-BACK-HALF-HANDOFF.md, hospitality.chelseafc.com requires existing paid-customer status to access the booking flow.
       - Step 10 — `enquiry-form-prefill` step. Mark `skipped: "requires-paid-account"`. NOT a `manual_chrome_mcp` (the user has no upgrade path until pilot-gate decision).
       - Step 11 — final summary screenshot of the deepest reachable page (top of the package landing).
       - Total step count: 8–11.
       - hide_selectors include `[id*="onetrust"]`, common chat widgets.
       - PRESERVE the `metadata.dead_ends` from the front-half crawl (deduplicated automatically by Plan 02-08's `_dedupe_meta`).
       - Set `metadata.broker_vendor: "keith_prowse"` (suspected per research §5; if Plan 02-12 later proves wrong, can be flipped). Document the inference in the JSON's top-level `notes` field if the schema supports it; if not, document in the SUMMARY for Plan 02-09.

    2. **Tests** (extend `scanner/tests/test_flow_schema_overrides.py`):
       - 1 test for chelsea step count.
       - 1 test for at-least-one `skipped: "requires-paid-account"` step.
       - 1 test for dead_ends dedup (`len(set(meta.dead_ends)) == len(meta.dead_ends)`).
       - 1 test for broker_vendor field set.

    3. **End-of-plan smoke test:**
       - Run `scanner flow validate` on all 5 hospitality flow-maps. All 5 must exit 0.
       - Add a single regression test that loops over `scanner/flow-maps/hospitality/*.json` and validates each. This guards against bit-rot when Plan 02-08's schema additions land.

    4. Commit:
       - `feat(02-09): chelsea flow-map (Option B partial, paid-customer steps marked skipped)`
       - `test(02-09): regression test loops all hospitality flow-maps through validate_flow_map`
  </action>
  <verify>
    <automated>cd scanner && for f in flow-maps/hospitality/*.json; do uv run python -m scanner flow validate "$f" || exit 1; done && uv run pytest tests/test_flow_schema_overrides.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>
    - chelsea.json has 8–11 steps with at least one `skipped: "requires-paid-account"`.
    - All 5 hospitality flow-maps validate.
    - All schema-override tests green.
    - `metadata.dead_ends` deduplicated.
    - Subscription cost: $0 (manual JSON authoring only).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flow-map JSON → capture orchestrator | Hand-authored JSON; if a malicious actor injected a step with selectors targeting credentials, the orchestrator would execute against a club site. |
| `requires_credentials: true` → credentials.get_credential | Trusted call into Plan 02-01 helper; helper never logs values. |
| `manual_chrome_mcp: true` → user instruction | Capture orchestrator (Plan 02-10) prompts the user with the URL + step name; user decides what to do. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Flow-map JSON content | mitigate | All 5 flow-maps committed to git, reviewed in PR. Schema validation forces structural sanity (D-15 / D-16). |
| T-09-02 | Information Disclosure | enquiry-form-prefill values | accept | All 5 flow-maps use D-10 dummy values (`Test Test`, `test@example.com`, `+44 0000 000000`) — no real PII. |
| T-09-03 | Spoofing | manual_chrome_mcp marker | accept | The marker is informational; user retains full control of the Chrome MCP session. No automated impersonation. |
| T-09-04 | Elevation of Privilege | requires_credentials → get_credential | mitigate | Plan 02-01 helper enforces value-leak-proof error messaging (T-02-01-01 / T-02-01-02). New marker doesn't change the contract. |
| T-09-05 | Denial of Service | skipped step capture orchestrator handling | accept | Plan 02-10's orchestrator skips these steps and records the skip reason. Bounded — never blocks the run. |

</threat_model>

<verification>
- [ ] All 5 hospitality flow-maps validate (`scanner flow validate <path>` exits 0).
- [ ] FlowStep schema accepts 3 new optional fields with correct defaults.
- [ ] D-16 closed Literal unchanged: `action: "submit"` still rejected.
- [ ] No real PII in any form_fields (D-10 dummy values only).
- [ ] At least one Chelsea step marked `skipped: "requires-paid-account"`.
- [ ] All 5 maps have `fixture_id: "FIRST_AVAILABLE"` placeholder per D-09.
- [ ] `git diff --quiet analysis/homepage/` exits 0.
- [ ] All scanner tests still green (175 + Plan 02-08 additions + Plan 02-09 ≈ 185 tests).
</verification>

<success_criteria>
- 5 production-ready flow-maps describing the FULL purchase path per club.
- Schema additive extensions: requires_credentials, manual_chrome_mcp, skipped.
- D-16 invariant intact (no submit action emitted anywhere).
- Subscription budget: $0.
- Wall-clock: ~3.75 hrs manual authoring + tests.
- All 5 flow-maps stage Plan 02-10 capture orchestration cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-hospitality-pilot/02-09-SUMMARY.md` with:
- Per-club step count + manual_chrome_mcp / requires_credentials / skipped marker counts.
- Schema additions changelog.
- Chelsea Option B partial confirmation: which steps marked skipped + rationale.
- All 5 flow-map validation results.
- Total tests added.
- Commits list.
</output>
