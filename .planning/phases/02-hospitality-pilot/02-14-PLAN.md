---
phase: 02-hospitality-pilot
plan: 14
type: execute
wave: 11
depends_on: [02-13]
files_modified:
  - .planning/phases/02-hospitality-pilot/02-PILOT-COVERAGE-REPORT.md
  - .planning/phases/02-hospitality-pilot/02-CLOSURE-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [HOSP-01, HOSP-02, HOSP-03]
must_haves:
  truths:
    - "A consolidated coverage report at `.planning/phases/02-hospitality-pilot/02-PILOT-COVERAGE-REPORT.md` exists with: per-feature coverage rate × 5 clubs, disagreement count, partial-Chelsea status, list of blocked steps with reasons"
    - "User reviews the coverage report + opens `scanner/output/contact-report-hospitality.html` + visits `/hospitality` route in dev (`npm run dev`); approval is gated on user typing the explicit phrase 'pilot approved' (D-17 hard gate)"
    - "On approval: HOSP-01, HOSP-02, HOSP-03 transition from `[ ]` to `[x]` in REQUIREMENTS.md traceability table; STATE.md and ROADMAP.md reflect Phase 2 closure; `.planning/phases/02-hospitality-pilot/02-CLOSURE-SUMMARY.md` written"
    - "On rejection: user feedback captured verbatim in STATE.md as a blocker; remediation candidate plans listed; phase remains paused with HOSP-* still `[ ]`"
    - "Phase 2.5 (full 28-club rollout) is the IMMEDIATE next gate but is NOT planned in this plan — it is a separate future phase"
  artifacts:
    - path: .planning/phases/02-hospitality-pilot/02-PILOT-COVERAGE-REPORT.md
      provides: "Consolidated coverage report for user gate review"
      min_lines: 50
    - path: .planning/phases/02-hospitality-pilot/02-CLOSURE-SUMMARY.md
      provides: "Phase 2 closure summary written ONLY if user approves the gate"
      min_lines: 40
    - path: .planning/REQUIREMENTS.md
      provides: "HOSP-01..HOSP-03 traceability table updated to [x] on approval"
      contains: "HOSP-01 | Phase 2 | v7 |"
    - path: .planning/STATE.md
      provides: "Phase 2 status flipped from 'paused — back-half pending' to either 'COMPLETE' (approval) or 'awaiting remediation' (rejection)"
      contains: "Phase 2"
    - path: .planning/ROADMAP.md
      provides: "Phase 2 marked complete with completion date; Phase 2.5 remains pending"
      contains: "Phase 2"
  key_links:
    - from: scanner/output/contact-report-hospitality.html (Plan 02-12)
      to: .planning/phases/02-hospitality-pilot/02-PILOT-COVERAGE-REPORT.md
      via: "Report consolidates: contact sheet thumbnail counts + Chelsea Option B partial status + disagreement count"
      pattern: "contact-report-hospitality"
    - from: analysis/hospitality/results/{club}.json (Plan 02-12)
      to: .planning/phases/02-hospitality-pilot/02-PILOT-COVERAGE-REPORT.md
      via: "Report tallies present/absent/disputed/skipped feature counts per club from results JSONs"
      pattern: "features.*disputed.*skipped"
    - from: app/hospitality/page.tsx (Plan 02-13)
      to: .planning/phases/02-hospitality-pilot/02-PILOT-COVERAGE-REPORT.md
      via: "Report includes a 'visit /hospitality in dev' instruction so user verifies the actual rendered matrix"
      pattern: "/hospitality"
---

<objective>
Hard user gate (D-17). The pilot must pass before Phase 2.5 begins. This plan:

1. **Generates `02-PILOT-COVERAGE-REPORT.md`** — consolidated, scannable summary of what was captured, what was disputed, what was skipped, and what was blocked.
2. **Pauses for user approval** — `checkpoint:human-verify` with explicit approval phrase `"pilot approved"` (mirrors Plan 02-02's `"approved — freeze candidates"` pattern).
3. **On approval:** marks HOSP-01..03 complete in REQUIREMENTS.md, updates STATE.md + ROADMAP.md to reflect Phase 2 closure, writes `02-CLOSURE-SUMMARY.md`.
4. **On rejection:** captures user feedback verbatim in STATE.md as a blocker, writes remediation-candidate-plans list, leaves HOSP-* status `[ ]`.

**Critical: Phase 2.5 (the full 28-club rollout) is the IMMEDIATE next gate but is NOT planned in this plan** — it is a separate future phase. The user must explicitly invoke `/gsd:plan-phase 2.5` after approval.

This is a thin checkpoint plan. No code changes. No subscription cost.

Source-decision traceability:
- D-17: hard pilot-gate user approval required.
- D-19: "Pilot: 5 clubs" labeling preserved through Phase 2.5.
- HOSP-01, HOSP-02, HOSP-03 closure mapping.
- 02-BACK-HALF-HANDOFF "Risks carried into back-half" — surface any unresolved items here for user decision (e.g. Chelsea swap decision, Opus bbox empirical outcome).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-hospitality-pilot/02-CONTEXT.md
@.planning/phases/02-hospitality-pilot/02-FRONT-HALF-SUMMARY.md
@.planning/phases/02-hospitality-pilot/02-BACK-HALF-HANDOFF.md
@.planning/phases/02-hospitality-pilot/02-08-PLAN.md
@.planning/phases/02-hospitality-pilot/02-09-PLAN.md
@.planning/phases/02-hospitality-pilot/02-10-PLAN.md
@.planning/phases/02-hospitality-pilot/02-11-PLAN.md
@.planning/phases/02-hospitality-pilot/02-12-PLAN.md
@.planning/phases/02-hospitality-pilot/02-13-PLAN.md
@scanner/output/contact-report-hospitality.html
@scanner/output/disagreements-hospitality.json
@analysis/hospitality/results/

<interfaces>
<!-- The coverage report is a markdown document; structure is the contract: -->

```markdown
# Phase 2 — Hospitality Pilot Coverage Report

**Generated:** 2026-MM-DD
**Pilot clubs:** Manchester City, Tottenham, Real Madrid, PSG, Chelsea
**Rubric:** 55 features (analysis/hospitality/HOSPITALITY-FLOW.md)

## Per-Club Summary

| Club | Captured Steps | Features Present | Features Absent | Features Disputed | Features Skipped | Score (raw) |
|------|----------------|------------------|------------------|-------------------|-------------------|-------------|
| ... |

## Per-Feature Coverage Rate

| Feature | Present in N/5 | Disputed in N/5 | Notes |
|---------|----------------|-------------------|-------|
| ... (55 rows) |

## Blocked Steps + Reasons

| Club | Step | Reason |
|------|------|--------|
| ... |

## Disagreement Inventory

(reads scanner/output/disagreements-hospitality.json)

## Chelsea Option B Partial Status

(narrative — what was captured, what was paid-customer-skipped, recommendation for Phase 2.5)

## User Action Required

To approve and close Phase 2:
1. Open `scanner/output/contact-report-hospitality.html` in a browser.
2. Run `npm run dev`, visit http://localhost:3000/hospitality.
3. Type `pilot approved` to close the gate, OR provide feedback to schedule remediation.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate consolidated coverage report</name>
  <files>
    .planning/phases/02-hospitality-pilot/02-PILOT-COVERAGE-REPORT.md
  </files>
  <action>
    1. **Read the source artifacts:**
       - 5 result JSONs at `analysis/hospitality/results/{club}.json` (each has `features`, `disputed_features`, `skipped_features`, `captured_steps`, `judge_agreement_rate`).
       - `scanner/output/disagreements-hospitality.json`.
       - `scanner/output/capture-run-log-hospitality-{club}-{ts}.json` (5 files from Plan 02-10).
       - `analysis/hospitality/features.ts` (55-feature definitions).
       - `scanner/output/opus-bbox-calibration.json` (Plan 02-08).

    2. **Generate `.planning/phases/02-hospitality-pilot/02-PILOT-COVERAGE-REPORT.md`** with the structure shown in <interfaces>:
       - **Per-Club Summary table:** for each club, count `features` true vs false, length of `disputed_features`, length of `skipped_features`, length of `captured_steps`. Compute raw score by summing weight × presence per feature (use weightYes when present, weightNo when absent — match `analysis/homepage/features.ts` scoring convention).
       - **Per-Feature Coverage Rate table:** for each of 55 features, count how many clubs (out of 5) have `features[<feature_key>] === true`, and how many have it in `disputed_features[]`. Add a `notes` column with the feature `name` from features.ts.
       - **Blocked Steps + Reasons table:** read each capture-run-log JSON; for any step with `status` in (`skipped`, `chrome-mcp`, `missing-credentials`, `error`), include a row with the reason.
       - **Disagreement Inventory:** count Opus-vs-Sonnet disagreements per club + total. Categorize: presence disagreements vs bbox disagreements (per Phase 1 plan 01-08 disagreement schema). Include the top 5 most-disputed feature keys across all clubs.
       - **Chelsea Option B Partial Status section:** narrative paragraph noting:
         - Main domain (chelseafc.com) credentials worked.
         - hospitality.chelseafc.com requires paid-customer status.
         - N steps marked `skipped: requires-paid-account`.
         - Recommendation for Phase 2.5: keep Chelsea partial (with explicit footnote in matrix UI) vs swap for Newcastle/Aston Villa (mid-tier stress-test club from research §1).
       - **Opus Bbox Calibration Summary:** 2-line note from `opus-bbox-calibration.json` decided_mode + IoU diff.
       - **Subscription Budget Consumed:** ~$5.50 retail equivalent (Plan 02-11 + Plan 02-08 ≈ ~$0.05 calibration).
       - **Coverage Math:** total feature-coverage rate (sum of present across all 5 clubs / 275 total cells) — single-line headline metric for the user gate decision.
       - **User Action Required block** (verbatim):
         ```
         To approve and close Phase 2:
         1. Open scanner/output/contact-report-hospitality.html in a browser. Visually scan all 55 features × 5 clubs.
         2. Run `npm run dev`, visit http://localhost:3000/hospitality. Spot-check the matrix is rendering present/absent values correctly.
         3. Type `pilot approved` (case-insensitive) to close the pilot gate and unlock Phase 2.5 planning.
         4. Or provide feedback (free-form text) — Plan 02-14 will record the rejection in STATE.md as a blocker and exit without closing the phase.
         ```

    3. **Commit:**
       - `docs(02-14): write 02-PILOT-COVERAGE-REPORT.md (consolidates plans 02-08 through 02-13 outputs)`
  </action>
  <verify>
    <automated>test -f ".planning/phases/02-hospitality-pilot/02-PILOT-COVERAGE-REPORT.md" && wc -l ".planning/phases/02-hospitality-pilot/02-PILOT-COVERAGE-REPORT.md" | awk '{ if ($1 < 80) { print "TOO SHORT: " $1; exit 1 } else print "OK: " $1 " lines" }' && grep -c "^|" ".planning/phases/02-hospitality-pilot/02-PILOT-COVERAGE-REPORT.md" | awk '{ if ($1 < 60) { print "TABLES TOO SHORT: " $1 " rows"; exit 1 } else print "OK: " $1 " table rows" }'</automated>
  </verify>
  <done>
    - Coverage report exists with all 6 required sections.
    - Per-club summary covers all 5 clubs.
    - Per-feature coverage table has all 55 rows.
    - Blocked-steps section reads from all 5 run-log JSONs.
    - User-action block has verbatim approval phrase.
    - Commit landed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Pilot-gate user approval</name>
  <what-built>
    Plans 02-08 through 02-13 shipped:
    - Crawler v2 (turnstile detector, trusted_subdomains, dead_ends dedupe, per-domain cookie override) + Opus bbox calibration + credentials.py CWD fix.
    - 5 extended flow-maps with full purchase-path coverage (where reachable).
    - Multi-step capture orchestrator (capture_flow) producing per-step PNGs + run-logs.
    - Two-judge vision wave: 5 clubs × ~3 steps × 2 judges = ~30 vision calls (subscription) producing per-club result JSONs + aggregated disagreements.
    - PIL slices into analysis/hospitality/evidence/features/, contact-sheet HTML, derived results JSONs, recomputed scores.
    - /hospitality route in Next.js with "Pilot: 5 clubs" label, single-orange-CTA invariant preserved, visual baseline + a11y check committed.

    Coverage report consolidating all of the above is at:
    `.planning/phases/02-hospitality-pilot/02-PILOT-COVERAGE-REPORT.md`
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/02-hospitality-pilot/02-PILOT-COVERAGE-REPORT.md` end-to-end.
    2. Open `scanner/output/contact-report-hospitality.html` in a browser. Scan all 55 features × 5 clubs:
       - Are present features mostly accurate vs the actual hospitality landing pages? Spot-check 3–5 features per club by clicking through to the source URL.
       - Are red-bordered absent cells justifiable? (e.g. Chelsea's paid-customer-gated features should ALL be absent.)
       - Are disputed cells (where Opus and Sonnet disagreed) flagged appropriately?
    3. Run `npm run dev`, visit `http://localhost:3000/hospitality`:
       - Does the matrix render? "Pilot: 5 clubs" label visible above?
       - Are scores plausible? (Tottenham should likely score highest given transparent pricing per research §5.)
       - Is the "Back to home" affordance present and NOT styled orange?
    4. Spot-check the Opus bbox calibration outcome in `scanner/output/opus-bbox-calibration.json`:
       - Did empirical calibration land on `css` or `native`? Either is acceptable; just confirm it ran.
    5. Decide:
       - **Approve:** type `pilot approved` (case-insensitive). Plan proceeds to Task 3 (closure).
       - **Reject:** describe what's wrong. Plan proceeds to Task 4 (remediation capture).
       - **Approve with caveats:** type `pilot approved with caveats: <details>`. Plan proceeds to Task 3 AND records the caveats in STATE.md as Phase 2.5 follow-ups.
  </how-to-verify>
  <resume-signal>Type `pilot approved` (case-insensitive) for full approval, OR `pilot approved with caveats: <details>`, OR free-form rejection feedback (any text not starting with `pilot approved`).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: ON APPROVAL — close Phase 2 (REQUIREMENTS, STATE, ROADMAP, CLOSURE-SUMMARY)</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/phases/02-hospitality-pilot/02-CLOSURE-SUMMARY.md
  </files>
  <action>
    **Run this task ONLY if Task 2 returned approval (full or with-caveats). On rejection, skip directly to Task 4.**

    1. **Update REQUIREMENTS.md traceability table:**
       - HOSP-01: `Pending` → `Complete`.
       - HOSP-02: `Pending` → `Complete`.
       - HOSP-03: `Pending` → `Complete`.
       - Verify HOSP-04 (Phase 2.5) remains `Pending`.

    2. **Update REQUIREMENTS.md v7 section** — flip the unchecked HOSP-03 box to `[x]`:
       - HOSP-03: `[ ]` → `[x]`.
       - HOSP-01 + HOSP-02 already `[x]` from Plan 02-13/02-12 partial completion; confirm both still `[x]`.

    3. **Update STATE.md:**
       - `status: paused` → `status: complete` (or `in_progress` if user wants to immediately move to Phase 2.5).
       - `stopped_at` → `Phase 2 COMPLETE: hospitality pilot approved 2026-MM-DD. HOSP-01..03 closed. Ready for Phase 2.5 (28-club rollout).`
       - `last_updated` → ISO8601 now.
       - `progress.completed_plans` → 14 (front-half 7 + back-half 7).
       - `progress.percent` → recompute: 14/15 = 93% (Phase 2.5 still pending).
       - Add a `## Pilot Approval Record` section logging: timestamp, approval phrase, any caveats from the user.

    4. **Update ROADMAP.md:**
       - Phase 2 row: `0/8` (or whatever it shows) → `14/14` plans complete + status `Complete`.
       - Phase 2 line: `[ ] **Phase 2: Hospitality Pilot**` → `[x] **Phase 2: Hospitality Pilot**`.
       - Inside the Phase 2 plan list: append all back-half plans 02-08 through 02-14 with `[x]` and brief objective. Mirror the front-half table format.
       - Phase 2.5 row remains `Not started`.
       - "Completed" date: today's ISO date.

    5. **Write `.planning/phases/02-hospitality-pilot/02-CLOSURE-SUMMARY.md`** (~60–100 lines):
       - **Phase 2 outcomes** — front + back half summarized in one place.
       - **Per-requirement closure** — HOSP-01/02/03 with how each was satisfied.
       - **Plans shipped** — 14 total (front-half 7 + back-half 7) with one-line objective each.
       - **Subscription budget consumed** — totals across the 14 plans (~$5.50 vision + ~$0.05 calibration).
       - **Wall-clock totals.**
       - **Pilot approval record** — verbatim phrase + any caveats.
       - **Carryover items for Phase 2.5** — explicit list (e.g. Newcastle/Aston Villa decision, headless-blocked clubs via Chrome MCP, Barcelona Socios overlay, locale expansion).
       - **Pointers** — to the coverage report, contact sheet, results JSONs, /hospitality route.

    6. **Commits:**
       - `docs(02-14): close Phase 2 — REQUIREMENTS HOSP-01..03 [x], STATE complete, ROADMAP updated`
       - `docs(02-14): write 02-CLOSURE-SUMMARY.md (Phase 2 closure record)`
  </action>
  <verify>
    <automated>grep -c "^| HOSP-0[123].*Complete" .planning/REQUIREMENTS.md | awk '{ if ($1 != 3) { print "EXPECTED 3 Complete rows, got " $1; exit 1 } else print "OK: 3 Complete" }' && grep -c "^- \[x\] HOSP-0[123]" .planning/REQUIREMENTS.md | awk '{ if ($1 < 3) { print "EXPECTED >=3 [x] HOSP-0X rows, got " $1; exit 1 } else print "OK" }' && test -f ".planning/phases/02-hospitality-pilot/02-CLOSURE-SUMMARY.md" && grep -q "complete" .planning/STATE.md && echo "Closure artifacts OK"</automated>
  </verify>
  <done>
    - REQUIREMENTS.md HOSP-01..03 all `Complete` + `[x]`.
    - STATE.md status flipped to `complete`; pilot approval record logged.
    - ROADMAP.md Phase 2 row updated to `[x]` with all 14 plans listed.
    - 02-CLOSURE-SUMMARY.md exists with all sections.
    - 2 commits landed.
  </done>
</task>

<task type="auto">
  <name>Task 4: ON REJECTION — capture remediation feedback in STATE.md</name>
  <files>
    .planning/STATE.md,
    .planning/phases/02-hospitality-pilot/02-PILOT-REJECTION-NOTES.md
  </files>
  <action>
    **Run this task ONLY if Task 2 returned rejection (any feedback text not starting with `pilot approved`).**

    1. **Capture user feedback** verbatim from Task 2's resume signal.

    2. **Write `.planning/phases/02-hospitality-pilot/02-PILOT-REJECTION-NOTES.md`** with:
       - User's verbatim feedback.
       - Inferred remediation candidates (e.g. "user said 'TOT scores are too low' → candidate plan 02-15: re-vision Tottenham with stricter prompt").
       - Suggested next step: `/gsd:plan-phase 2 --gaps` to plan remediation.

    3. **Update STATE.md:**
       - `status: paused` → `status: paused (pilot rejected)`.
       - `stopped_at` → `Pilot gate rejected 2026-MM-DD. Remediation pending. See 02-PILOT-REJECTION-NOTES.md.`
       - Add a `## Blockers` entry: `Phase 2 pilot gate failed. User feedback: <verbatim>. Remediation candidates: <list>.`
       - HOSP-01..03 remain `[ ]` / `Pending`.
       - ROADMAP.md NOT changed (Phase 2 stays in progress).

    4. **Commit:** `docs(02-14): record pilot rejection feedback + remediation candidates in STATE.md + notes file`.

    5. **End-of-plan posture:** STOP. Do NOT proceed to Task 3. The phase remains paused until user invokes a remediation plan (`/gsd:plan-phase 2 --gaps`).
  </action>
  <verify>
    <automated>test -f ".planning/phases/02-hospitality-pilot/02-PILOT-REJECTION-NOTES.md" && grep -q "Blocker" .planning/STATE.md && grep -c "^- \[ \] HOSP-0[123]" .planning/REQUIREMENTS.md | awk '{ if ($1 < 3) { print "EXPECTED HOSP-* still unchecked, got " $1; exit 1 } else print "OK: HOSP-* still pending" }'</automated>
  </verify>
  <done>
    - 02-PILOT-REJECTION-NOTES.md exists with verbatim feedback + remediation candidates.
    - STATE.md has Blocker entry + status reflects rejection.
    - HOSP-01..03 remain `Pending` / `[ ]`.
    - 1 commit landed.
    - Phase 2 remains paused.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → approval phrase parser | User-supplied free-form string; case-insensitive prefix match `pilot approved`. |
| REQUIREMENTS.md / STATE.md / ROADMAP.md → git | Committed planning artifacts; standard PR-style review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Spoofing | Approval phrase | mitigate | Case-insensitive prefix match; `pilot approved` is intentionally specific (not a single word) to prevent accidental approvals from chat noise. Pattern: `re.match(r"^pilot approved\b", user_signal, re.IGNORECASE)`. |
| T-14-02 | Tampering | Direct edits to REQUIREMENTS.md without approval | accept | Tasks 3 / 4 dispatch is gated on Task 2's resume signal. If a developer hand-edits REQUIREMENTS.md outside this flow, the standard git PR review catches it. |
| T-14-03 | Information Disclosure | User feedback verbatim in committed file | accept | The user provides the feedback knowing it will be committed. No third-party PII. |
| T-14-04 | Repudiation | Pilot approval audit trail | mitigate | The verbatim approval phrase + timestamp are logged in STATE.md AND 02-CLOSURE-SUMMARY.md. Two-place record. |

</threat_model>

<verification>
- [ ] 02-PILOT-COVERAGE-REPORT.md exists with all 6 required sections.
- [ ] Task 2 user-gate runs (Auto Mode pauses for human input).
- [ ] On approval: REQUIREMENTS.md / STATE.md / ROADMAP.md updated; 02-CLOSURE-SUMMARY.md exists.
- [ ] On rejection: 02-PILOT-REJECTION-NOTES.md exists; STATE.md has Blocker entry; HOSP-* remain `[ ]`.
- [ ] No code changes (this plan is pure documentation + state management).
- [ ] D-20 invariant holds (no analysis/homepage changes).
- [ ] D-21 invariant holds (no scanner internals changes).
- [ ] Commits use `docs(02-14):` convention per CLAUDE.md.
</verification>

<success_criteria>
- Pilot gate executed.
- On approval: Phase 2 closed, HOSP-01..03 marked complete, ROADMAP shows Phase 2 done, Phase 2.5 ready to plan.
- On rejection: feedback captured, blockers logged, remediation candidates surfaced for `/gsd:plan-phase 2 --gaps`.
- Subscription cost: $0.
- Wall-clock: minutes (mostly user review time).
</success_criteria>

<output>
Plan 02-14 either:

1. **Closure path:** writes `.planning/phases/02-hospitality-pilot/02-CLOSURE-SUMMARY.md` (Task 3) and updates planning state files. Phase 2 is closed.

2. **Rejection path:** writes `.planning/phases/02-hospitality-pilot/02-PILOT-REJECTION-NOTES.md` (Task 4) and records blockers. Phase 2 stays paused.

After execution, no follow-on PLAN-SUMMARY.md is needed beyond the artifacts above (the Closure or Rejection note IS the plan's summary).
</output>
